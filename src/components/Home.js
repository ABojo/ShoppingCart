const textStyles = {
  fontSize: '3rem',
  fontWeight: '300',
  textAlign: 'center',
  textTransform: 'uppercase',
};

function Home() {
  return <h1 style={textStyles}>Home page content</h1>;
}

export default Home;
